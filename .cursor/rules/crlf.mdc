---
description: 仓库文本文件统一使用 CRLF 换行，每次落盘后强制校验
alwaysApply: true
---

# 换行符（强制）

- 一律 CRLF（`\r\n`），禁止 LF（`\n`）
- 依据根目录 `.editorconfig` 的 `end_of_line = crlf`
- **Write / StrReplace / EditNotebook 落盘后必须立刻校验并转成 CRLF**，不得等用户提醒
- 校验：读文件字节，若存在孤立 `\n`（不在 `\r\n` 中），立刻把 `\n` 全部换成 `\r\n` 再写回
- 新建文件尤其容易被工具写成 LF，**新增文件必须作为强制检查对象**
- 同一轮改了多个文件时，对本轮所有新建/改写的文本文件逐一处理，禁止只转其中一个
